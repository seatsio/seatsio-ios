name: 'Release'

on:
  release:
    types: [created]

jobs:
  build:

    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2
      - run: pod install
      - run: pod lib lint
      - run: pud trunk push