public enum EventMode: String, Encodable {
    case manageObjectStatuses
    case manageForSaleConfig
    case manageTableBooking
    case manageChannels
    case manageCategories
    case select
    case filterSections
    case `static`
}
